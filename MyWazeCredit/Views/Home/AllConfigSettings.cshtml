@model List<string>

<div class="p-4">
    <h3> All config value form teh AppSettings:</h3>

    <ul>
        @foreach (var message in Model)
        {
            <li>@message</li>
        }

    </ul>

</div>